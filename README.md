Quick shell script to print the password of a saved wifi connection. Prints password for current connection, or the one specified via the first argument.

*Uses sudo*

## Requirements

- NetworkManager as your network manager
- `iwgetid`
- `sed`
- `grep`
- `cat`
